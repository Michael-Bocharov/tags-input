import { SkipSpaceDirective } from './skip-space.directive';

describe('SkipSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new SkipSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
